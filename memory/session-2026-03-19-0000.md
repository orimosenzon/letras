# Session 2026-03-19

## What was done
- Added **lrc_status badges** on search result cards
  - `_check_lrclib()` now returns `"synced"` / `"plain"` / `"none"` (was bool)
  - `search_songs()` stores `lrc_status` per candidate + keeps `has_lrc` for backward compat
  - Sort order: synced → plain → none
  - Frontend: badge overlay (top-left on thumbnail) — ♪ Synced (teal), ♪ Lyrics (gray), ? Unknown (dark)
  - CSS: `.song-card { position: relative }` + badge classes

## Next steps
1. **תיקון performer score ב-MusicBrainz** — performer לפעמים לא נכון
2. **בדיקת איכות lyrics.ovh** — האם שירים מ-lyrics.ovh באיכות סבירה
3. **Cache לתוצאות חיפוש** — חיסכון בקריאות YouTube + LRClib
