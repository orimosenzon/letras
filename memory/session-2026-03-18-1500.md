# Session 2026-03-18-1500

## מה נעשה

### Wikipedia modal — הרחבה לאנשים ולשיר
- לחיצה על כל שם בקרדיטים (מילים, לחן, עיבוד, מבצע) פותחת wiki modal עם מידע על אותו אדם
- לחיצה על כותרת השיר פותחת wiki modal על השיר
- המודל מביא עכשיו תוכן מלא (MediaWiki API, עד 8000 תווים) במקום רק תקציר קצר
- ה-footer (תרגום + לינק) נשאר קבוע למטה בזמן גלילה

### שיפור תוצאות החיפוש
- תוצאות חיפוש מציגות עד 8 שירים (במקום 5 LRClib בלבד)
- שירים עם LRClib מסומנים עם glow ירוק-כחול
- שירים ללא LRClib מוצגים dim (opacity 0.65) אבל לחיצים

### fallback למילים ללא סנכרון
- שרשרת fallback: YouTube captions → LRClib synced → LRClib plain → lyrics.ovh
- אם אין מילים כלל — הסרטון עדיין מתנגן עם הודעה "♪ No lyrics found"
- שירים plain-only מוצגים עם הודעה "♪ lyrics only — no sync", ללא karaoke highlight

## Next Steps
1. בדוק שירים שמגיעים מ-lyrics.ovh (האם האיכות מספקת?)
2. שקול להוסיף badge/אייקון על הכרטיסיות (סנכרון / מילים בלבד / אין מילים) לפני לחיצה
3. Cache לתוצאות החיפוש — כרגע כל חיפוש קורא ל-YouTube + LRClib במקביל
4. תיקון performer score ב-MusicBrainz (עדיין מחזיר performer שגוי לפעמים)
